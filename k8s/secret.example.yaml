apiVersion: v1
kind: Secret
metadata:
  name: agent-secrets
  namespace: financial-agents
type: Opaque
stringData:
  # CHANGE BEFORE PRODUCTION — use: kubectl create secret generic agent-secrets --from-env-file=.env
  POSTGRES_USER: finagent
  POSTGRES_PASSWORD: CHANGE_ME_STRONG_PASSWORD
  POSTGRES_DB: financial_gold
